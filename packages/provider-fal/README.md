# @fal-tools/provider-fal

The fal.ai queue adapter and model capability registry for `fal-tools`. It uses
the current `@fal-ai/client`, downloads generated media without retaining signed
URLs, and never supplies pricing unless the caller configures sourced,
timestamped pricing.
