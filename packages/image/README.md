# @fal-tools/image

Objective image processing and QA for `fal-tools`, implemented with the optional
`sharp` dependency. Checks cover dimensions, alpha presence, alpha bounding box,
and duplicate content hashes. No semantic quality claims are made.
