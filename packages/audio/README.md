# @fal-tools/audio

Audio transcoding and objective technical inspection for fal-tools. This package
discovers system `ffmpeg` and `ffprobe` executables on `PATH` and does not
download or bundle media binaries.
